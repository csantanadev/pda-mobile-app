import { StyleSheet } from 'react-native';

export default StyleSheet.create({

    container: {
        justifyContent: "center",
        fontWeight: 'bold',
        marginTop: '50%',
        alignItems: 'center',
        backgroundColor: '#FFF',
        height: 300
    },
    textLoading: {
        fontSize: 16,
        color: "#616161"
    }
});

